version: "3"
services:
  nginx:
    image: nginx:1.21.6-alpine
    restart: always
    depends_on:
      - "transcendence-app"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro

  transcendence-app:
    build: ./transcendence-app
    restart: always
    depends_on:
      - "db"

  db:
    image: postgres:14.2-alpine3.15
    restart: always
    volumes:
      - transcendence-db:/var/lib/postgresql/data

volumes:
  transcendence-db:
