export enum Color {
  WARNING = '--color-warning',
  SUBMIT = '--color-submit',
  ONLINE = '--color-online',
  OFFLINE = '--color-offline',
  LIGHT = '--color-light',
  BACKGROUND = '--color-background',
  DARK = '--color-dark',
}
