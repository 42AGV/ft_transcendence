{
  "private": true,
  "name": "transcendence-monorepo",
  "workspaces": [
    "packages/pong-engine",
    "transcendence-app",
    "webapp"
  ],
  "scripts": {
    "build-packages": "npm i -w packages/* && npm run build -w packages/*"
  }
}